Codes to pull correct ISCE files for MSBAS and then produce necessary input files

updated_move_script.py copies files on summit needed for msbas to a download dir - inputs are the working directory and optional --rm_flag

PrepMsbasIfgs_jh.py creates input files needed for MSBAS algorithm. necessary file structure follows on from above script.

inputs.txt example input file for Prep script (put location of inputs file in cmd line)
